<source>
  @type sample
  sample {"data":"test"}
  tag sample
</source>

<filter test>
  @type record_transformer
  enable_ruby
  <record>
    time ${time rescue nil}
  </record>
</filter>

<match sample>
  @type bigquery_insert_storage_write

  auth_method application-default

  project sample-project
  dataset test
  table data

  generated_schema_rb_path proto/test_data_pb.rb
</match>
